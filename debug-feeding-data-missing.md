# Debug Session: feeding-data-missing

状态：OPEN

## 问题

用户反馈：今天的喂养数据好像丢了一部分。

## 约束

- 在获得运行时证据前，不修改业务逻辑。
- 第一处现有代码改动必须只加入调试埋点。
- 修复后需要用修复前/修复后证据对比验证。

## 初始假设

1. 当前选中的宝宝变了，统计页正在查询另一个 `babyId`，所以看起来今天数据缺失。
2. 部分记录还在前端 `localStorage.pending_records`，后端数据库没有同步成功。
3. 日期边界或时区导致「今天」查询范围不覆盖部分记录。
4. 后端记录接口返回正常，但前端事件类型映射或列表展示过滤导致看起来缺失。
5. 最近家庭协作改造后，部分记录写入到了另一个宝宝或另一个用户关联下。

## 待收集证据

- 当前登录用户、当前宝宝 ID。
- 今天统计页实际请求的 `babyId/from/to/date`。
- 后端 `/api/records` 返回条数与数据库中同范围记录条数。
- 浏览器本地 `pending_records` 数量。

## 时间线

- 初始化调试会话。
